{
  "name": "pi-ttft",
  "version": "1.1.0",
  "description": "Time-to-first-token HUD for the Pi coding agent \u2014 stays minimal until something is slow, then names the cause (network vs server prefill).",
  "type": "module",
  "license": "MIT",
  "keywords": [
    "pi-package",
    "pi",
    "ttft",
    "latency",
    "observability",
    "time-to-first-token"
  ],
  "files": [
    "extensions",
    "README.md",
    "LICENSE"
  ],
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "peerDependenciesMeta": {
    "@earendil-works/pi-coding-agent": {
      "optional": true
    }
  },
  "engines": {
    "node": ">=18"
  }
}
